//echo 1 print its command line arguments
package main

import (
  "fmt"
  "os"
)

func main () {
  start := 1
  counter := 0
  w


}

func HandlerSock() {
  conn := socket.AF_INET( argv[:1], exec('tracert argv[1]'))
  fmt.Fprintf(r, conn)
  if conn != socket.Status(404) {

  }
}
